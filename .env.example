# BGP Monitor v2.0 - Configurações do Sistema
# Copie este arquivo para .env e configure com seus valores

# === CONFIGURAÇÕES DO BANCO DE DADOS POSTGRESQL ===
# Nome do banco de dados PostgreSQL
DATABASE_NAME=bgp_monitor

# Usuário do banco de dados PostgreSQL
DATABASE_USER=bgp_monitor

# Senha do usuário do banco (altere para uma senha segura)
DATABASE_PASSWORD=bgp_monitor_password

# Host do banco de dados (localhost para instalação local)
DATABASE_HOST=localhost

# Porta do PostgreSQL (padrão: 5432)
DATABASE_PORT=5432

# Pool de conexões do banco
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# === CONFIGURAÇÕES DA API RIPE ===
# URL base da API RIPE NCC
RIPE_API_URL=https://stat.ripe.net/data

# Rate limiting para não ser bloqueado pela API
# Tempo mínimo entre requisições para o mesmo ASN (segundos)
API_RATE_LIMIT_PER_ASN=45

# Quantidade de ASNs processados por lote
API_BATCH_SIZE=3

# Timeout para requisições HTTP (segundos)
HTTP_TIMEOUT=30

# === CONFIGURAÇÕES DE RETENÇÃO DE DADOS ===
# Período de retenção dos dados históricos (dias)
DATA_RETENTION_DAYS=365

# Executar limpeza automática de dados antigos
AUTO_CLEANUP_ENABLED=true

# === CONFIGURAÇÕES DO DETECTOR DE ANOMALIAS ===
# Período mínimo de dados para baseline (horas)
ANOMALY_MIN_BASELINE_HOURS=24

# Multiplicador Z-score para detecção de anomalias
ANOMALY_ZSCORE_THRESHOLD=2.5

# Percentual mínimo de mudança para considerar anomalia
ANOMALY_MIN_CHANGE_PERCENT=20

# === CONFIGURAÇÕES DO SCHEDULER ===
# Intervalo entre coletas automáticas (minutos)
SCHEDULER_INTERVAL_MINUTES=5

# Executar scheduler automaticamente no startup
SCHEDULER_AUTO_START=true

# === CONFIGURAÇÕES DO TELEGRAM (OPCIONAL) ===
# Token do bot do Telegram (obtenha com @BotFather)
TELEGRAM_BOT_TOKEN=

# ID do chat/canal para enviar alertas
TELEGRAM_CHAT_ID=

# Habilitar alertas do Telegram
TELEGRAM_ALERTS_ENABLED=false

# === CONFIGURAÇÕES DA API WEB ===
# Porta para a API REST
API_PORT=8000

# Host para bind da API (0.0.0.0 para todas as interfaces)
API_HOST=0.0.0.0

# Modo debug (apenas para desenvolvimento)
DEBUG_MODE=false

# === CONFIGURAÇÕES DE LOG ===
# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Arquivo de log (deixe vazio para apenas console)
LOG_FILE=

# Rotação de logs (tamanho máximo em MB)
LOG_MAX_SIZE_MB=10

# Quantidade de arquivos de backup de log
LOG_BACKUP_COUNT=5
